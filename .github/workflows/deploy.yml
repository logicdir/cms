name: LogicDir CMS Deployment

on:
  push:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
      - name: Install Dependencies
        run: composer install -q --no-ansi --no-interaction --no-scripts --no-progress --prefer-dist
      - name: Run Tests
        run: php artisan test

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.event_name == 'push'
    steps:
      - uses: actions/checkout@v3
      - name: Validate Secrets
        run: |
          if [ -z "${{ secrets.SFTP_HOST }}" ]; then echo "❌ SFTP_HOST is missing"; exit 1; fi
          if [ -z "${{ secrets.SFTP_USER }}" ]; then echo "❌ SFTP_USER is missing"; exit 1; fi
          if [ -z "${{ secrets.SFTP_KEY }}" ]; then echo "❌ SFTP_KEY is missing"; exit 1; fi
      - name: Build Assets
        run: |
          npm install
          npm run build
      - name: Create Package
        run: zip -r deploy-package.zip . -x ".git/*" "node_modules/*"
      - name: Deploy to Server via SFTP
        uses: wlixcc/SFTP-Deploy-Action@v1.2.4
        with:
          username: ${{ secrets.SFTP_USER }}
          server: ${{ secrets.SFTP_HOST }}
          ssh_private_key: ${{ secrets.SFTP_KEY }}
          local_path: 'deploy-package.zip'
          remote_path: '/home/${{ secrets.SFTP_USER }}/deployments'
          port: 22
          args: '-o ConnectTimeout=5'
